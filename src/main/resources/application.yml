spring:
  datasource:
    url: jdbc:mysql://localhost:3306/club_db
    username: root
    password: 본인비밀번호
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true